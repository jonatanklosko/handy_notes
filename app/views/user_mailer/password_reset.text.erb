Hi <%= @user.username %>

We've heard that You've forgotten Your password.
That's no problem, just click the link below to change
your password.

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

If You don't want to reset the password, ignore this message.
The reset link will expire in 24 hours.

If You want to reset the password but the link has expired,
go to <%= new_password_reset_url %> to get new one.